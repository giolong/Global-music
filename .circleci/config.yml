version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.

docker:
      - image: orus/hugo-builder

jobs:
  build:
    branches:
      only:
        - master
